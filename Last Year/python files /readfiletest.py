def main():kCount=0
    while kcount==0: try: filename=
    raw_input ("Enter the name of a file") f= open(filename,"r") count=0
    for line in A:
    line=f.readline()
    line= line.lower()
    if string.find('a')
    kCount=kCount+line.count('a')
